function natesChain() {
  var gold = 10;
  var silver = 20;
  var answer = gold + silver;
  return answer;
  console.log(answer);
}
console.log(natesChain());
function addNumber(num1, num2) {
  var ans = num1 + num2;
  return ans;
}
var result = addNumber(11, 9);
console.log(result);

function natesChainz(gold, silver) {
  console.log(`nate has ${gold} gold chains and ${silver} silver chains`);
}

const num = parseInt(
  prompt(`CARRIER INFO
0:Nyakua bonus
1:Buy Data Deals
2:Daily Bundles new
3:Weekly Bundles New
4:Monthly Bundles New
5:No Expirey Bundles
6:Entertainment Bundles
7:Okoa Data
8:Lipa mdogo mdogo
98:More
`)
);
if (num === 0) {
  const ex = parseInt(
    prompt(`Nyakua Bonus
    1:Buy Nyakua Bundles
    2:Check My Target
    3:Check my Daily Usage
    4:Check my FREE Data Balance
    5:Join Data Bonus
    6:Opt Out
    7:How it Works
    0:BACK 00:HOME
    `)
  );
  if (ex === 1) {
    alert(`${ex}:Buy Nyakua Bundels`);
  } else if (ex === 2) {
    alert(`${ex}:Check my Target`);
  } else if (ex === 3) {
    alert(`${ex}:Check My Daily Usage`);
  } else if (ex === 4) {
    alert(`${ex}:Check my FREE Data Balance`);
  } else if (ex === 5) {
    alert(`${ex}:Join Data Bonus`);
  } else if (ex === 6) {
    alert(`${ex}:Opt Out`);
  } else if (ex === 7) {
    alert(`${ex}:How it Works`);
  } else if (ex === 0) {
    alert(`${ex}:BACK`);
  } else if (ex === 00) {
    alert(`${ex}:HOME`);
  } else "please enter a valid number";
} // option 0 ends here
else if (num === 1) {
  const one = parseInt(
    prompt(`
    1:Sh 20 = 1gb for 1hr
    2:Sh 45 = 400mb for 24hr
    3:Sh 1000 = 15gb for 30days
    4:Sh 1500 = 25gb for 30days
    5:Sh 2500 = 90gb for 30days(3gb/Day)
    6:Hot Deals🔥🔥(Tunukiwa)
    `)
  );
  if (one === 1) {
    alert(`${one}:Confirm that you want to buy 1gb for 1hr`);
  } else if (one === 2) {
    alert(`${one}:Confirm that you want to buy 400mb for 24hr`);
  } else if (one === 3) {
    alert(`${one}:Confirm that you want to buy 15gb for 30days`);
  } else if (one === 4) {
    alert(`${one}:Confirm that you want to buy 25gb for 30days`);
  } else if (one === 5) {
    alert(`${one}:Confirm that you want to buy 90gb for 30days(3gb/Day)`);
  } else if (one === 6) {
    alert(`${one}:HOT DEALS🔥🔥`);
  } else "please enter a valid number";
} // option1 ends here
else if (num === 2) {
  const one = parseInt(
    prompt(`
    1:Sh 20 = 1gb for 1hr
    2:Sh 45 = 400mb for 24hr
    3:Sh 1000 = 15gb for 30days
    4:Sh 1500 = 25gb for 30days
    5:Sh 2500 = 90gb for 30days(3gb/Day)
    6:Hot Deals🔥🔥(Tunukiwa)
    `)
  );
  if (one === 1) {
    alert(`${one}:Confirm that you want to buy 1gb for 1hr`);
  } else if (one === 2) {
    alert(`${one}:Confirm that you want to buy 400mb for 24hr`);
  } else if (one === 3) {
    alert(`${one}:Confirm that you want to buy 15gb for 30days`);
  } else if (one === 4) {
    alert(`${one}:Confirm that you want to buy 25gb for 30days`);
  } else if (one === 5) {
    alert(`${one}:Confirm that you want to buy 90gb for 30days(3gb/Day)`);
  } else if (one === 6) {
    alert(`${one}:HOT DEALS🔥🔥`);
  } else "please enter a valid number";
} //option 2 ends here
else if (num === 3) {
  const one = parseInt(
    prompt(`
    1:Sh 20 = 1gb for 1hr
    2:Sh 45 = 400mb for 24hr
    3:Sh 1000 = 15gb for 30days
    4:Sh 1500 = 25gb for 30days
    5:Sh 2500 = 90gb for 30days(3gb/Day)
    6:Hot Deals🔥🔥(Tunukiwa)
    `)
  );
  if (one === 1) {
    alert(`${one}:Confirm that you want to buy 1gb for 1hr`);
  } else if (one === 2) {
    alert(`${one}:Confirm that you want to buy 400mb for 24hr`);
  } else if (one === 3) {
    alert(`${one}:Confirm that you want to buy 15gb for 30days`);
  } else if (one === 4) {
    alert(`${one}:Confirm that you want to buy 25gb for 30days`);
  } else if (one === 5) {
    alert(`${one}:Confirm that you want to buy 90gb for 30days(3gb/Day)`);
  } else if (one === 6) {
    alert(`${one}:HOT DEALS🔥🔥`);
  } else "please enter a valid number";
} // option2 ends here
else if (num === 3) {
  const one = parseInt(
    prompt(`
    1:Sh 20 = 1gb for 1hr
    2:Sh 45 = 400mb for 24hr
    3:Sh 1000 = 15gb for 30days
    4:Sh 1500 = 25gb for 30days
    5:Sh 2500 = 90gb for 30days(3gb/Day)
    6:Hot Deals🔥🔥(Tunukiwa)
    `)
  );
  if (one === 1) {
    alert(`${one}:Confirm that you want to buy 1gb for 1hr`);
  } else if (one === 2) {
    alert(`${one}:Confirm that you want to buy 400mb for 24hr`);
  } else if (one === 3) {
    alert(`${one}:Confirm that you want to buy 15gb for 30days`);
  } else if (one === 4) {
    alert(`${one}:Confirm that you want to buy 25gb for 30days`);
  } else if (one === 5) {
    alert(`${one}:Confirm that you want to buy 90gb for 30days(3gb/Day)`);
  } else if (one === 6) {
    alert(`${one}:HOT DEALS🔥🔥`);
  } else "please enter a valid number";
} // option2 ends here
else if (num === 4) {
  const one = parseInt(
    prompt(`
    1:Sh 20 = 1gb for 1hr
    2:Sh 45 = 400mb for 24hr
    3:Sh 1000 = 15gb for 30days
    4:Sh 1500 = 25gb for 30days
    5:Sh 2500 = 90gb for 30days(3gb/Day)
    6:Hot Deals🔥🔥(Tunukiwa)
    `)
  );
  if (one === 1) {
    alert(`${one}:Confirm that you want to buy 1gb for 1hr`);
  } else if (one === 2) {
    alert(`${one}:Confirm that you want to buy 400mb for 24hr`);
  } else if (one === 3) {
    alert(`${one}:Confirm that you want to buy 15gb for 30days`);
  } else if (one === 4) {
    alert(`${one}:Confirm that you want to buy 25gb for 30days`);
  } else if (one === 5) {
    alert(`${one}:Confirm that you want to buy 90gb for 30days(3gb/Day)`);
  } else if (one === 6) {
    alert(`${one}:HOT DEALS🔥🔥`);
  } else "please enter a valid number";
} // option2 ends here
else if (num === 5) {
  const one = parseInt(
    prompt(`
    1:Sh 20 = 1gb for 1hr
    2:Sh 45 = 400mb for 24hr
    3:Sh 1000 = 15gb for 30days
    4:Sh 1500 = 25gb for 30days
    5:Sh 2500 = 90gb for 30days(3gb/Day)
    6:Hot Deals🔥🔥(Tunukiwa)
    `)
  );
  if (one === 1) {
    alert(`${one}:Confirm that you want to buy 1gb for 1hr`);
  } else if (one === 2) {
    alert(`${one}:Confirm that you want to buy 400mb for 24hr`);
  } else if (one === 3) {
    alert(`${one}:Confirm that you want to buy 15gb for 30days`);
  } else if (one === 4) {
    alert(`${one}:Confirm that you want to buy 25gb for 30days`);
  } else if (one === 5) {
    alert(`${one}:Confirm that you want to buy 90gb for 30days(3gb/Day)`);
  } else if (one === 6) {
    alert(`${one}:HOT DEALS🔥🔥`);
  } else "please enter a valid number";
} // option2 ends here
else if (num === 6) {
  const one = parseInt(
    prompt(`
    1:Sh 20 = 1gb for 1hr
    2:Sh 45 = 400mb for 24hr
    3:Sh 1000 = 15gb for 30days
    4:Sh 1500 = 25gb for 30days
    5:Sh 2500 = 90gb for 30days(3gb/Day)
    6:Hot Deals🔥🔥(Tunukiwa)
    `)
  );
  if (one === 1) {
    alert(`${one}:Confirm that you want to buy 1gb for 1hr`);
  } else if (one === 2) {
    alert(`${one}:Confirm that you want to buy 400mb for 24hr`);
  } else if (one === 3) {
    alert(`${one}:Confirm that you want to buy 15gb for 30days`);
  } else if (one === 4) {
    alert(`${one}:Confirm that you want to buy 25gb for 30days`);
  } else if (one === 5) {
    alert(`${one}:Confirm that you want to buy 90gb for 30days(3gb/Day)`);
  } else if (one === 6) {
    alert(`${one}:HOT DEALS🔥🔥`);
  } else "please enter a valid number";
} // option2 ends here
else if (num === 7) {
  const one = parseInt(
    prompt(`
    1:Sh 20 = 1gb for 1hr
    2:Sh 45 = 400mb for 24hr
    3:Sh 1000 = 15gb for 30days
    4:Sh 1500 = 25gb for 30days
    5:Sh 2500 = 90gb for 30days(3gb/Day)
    6:Hot Deals🔥🔥(Tunukiwa)
    `)
  );
  if (one === 1) {
    alert(`${one}:Confirm that you want to buy 1gb for 1hr`);
  } else if (one === 2) {
    alert(`${one}:Confirm that you want to buy 400mb for 24hr`);
  } else if (one === 3) {
    alert(`${one}:Confirm that you want to buy 15gb for 30days`);
  } else if (one === 4) {
    alert(`${one}:Confirm that you want to buy 25gb for 30days`);
  } else if (one === 5) {
    alert(`${one}:Confirm that you want to buy 90gb for 30days(3gb/Day)`);
  } else if (one === 6) {
    alert(`${one}:HOT DEALS🔥🔥`);
  } else "please enter a valid number";
} // option2 ends here
else if (num === 8) {
  const one = parseInt(
    prompt(`
    1:Sh 20 = 1gb for 1hr
    2:Sh 45 = 400mb for 24hr
    3:Sh 1000 = 15gb for 30days
    4:Sh 1500 = 25gb for 30days
    5:Sh 2500 = 90gb for 30days(3gb/Day)
    6:Hot Deals🔥🔥(Tunukiwa)
    `)
  );
  if (one === 1) {
    alert(`${one}:Confirm that you want to buy 1gb for 1hr`);
  } else if (one === 2) {
    alert(`${one}:Confirm that you want to buy 400mb for 24hr`);
  } else if (one === 3) {
    alert(`${one}:Confirm that you want to buy 15gb for 30days`);
  } else if (one === 4) {
    alert(`${one}:Confirm that you want to buy 25gb for 30days`);
  } else if (one === 5) {
    alert(`${one}:Confirm that you want to buy 90gb for 30days(3gb/Day)`);
  } else if (one === 6) {
    alert(`${one}:HOT DEALS🔥🔥`);
  } else "please enter a valid number";
} // option2 ends here
else if (num === 98) {
  const one = parseInt(
    prompt(`
    1:Sh 20 = 1gb for 1hr
    2:Sh 45 = 400mb for 24hr
    3:Sh 1000 = 15gb for 30days
    4:Sh 1500 = 25gb for 30days
    5:Sh 2500 = 90gb for 30days(3gb/Day)
    6:Hot Deals🔥🔥(Tunukiwa)
    `)
  );
  if (one === 1) {
    alert(`${one}:Confirm that you want to buy 1gb for 1hr`);
  } else if (one === 2) {
    alert(`${one}:Confirm that you want to buy 400mb for 24hr`);
  } else if (one === 3) {
    alert(`${one}:Confirm that you want to buy 15gb for 30days`);
  } else if (one === 4) {
    alert(`${one}:Confirm that you want to buy 25gb for 30days`);
  } else if (one === 5) {
    alert(`${one}:Confirm that you want to buy 90gb for 30days(3gb/Day)`);
  } else if (one === 6) {
    alert(`${one}:HOT DEALS🔥🔥`);
  } else "please enter a valid number";
} // option2 ends here
else "please enter a valid number";

//2
// const marks = parseInt(prompt("Student 1 enter your marks"));
// const marks2 = parseInt(prompt("Student 2 eenter your marks"));
// const marks3 = parseInt(prompt("Student3 nter your marks"));
// if (marks > marks2 && marks > marks3) {
//   alert(`${marks} is the highest grade`);
// } else if (marks2 > marks && marks2 > marks3) {
//   alert(`${marks} is the highest grade`);
// } else if (marks3 > marks && marks3 > marks2) {
//   alert(`${marks} is the highest grade`);
// } else {
//   ("you entered and invalid mark");
// }
// function marksChecker() {

//   console.log(high);
// }
// marksChecker(marks, marks2, marks3);
// console.log(marksZao);
function marksZao() {
  let zote = [];
  for (let i = 0; i < 3; i++) {
    const marks = parseInt(prompt(`please enter your marks`));
    if (marks >= 0 && marks <= 100) {
      zote.push(marks);
    }
  }
  console.log(
    `max value is : ${Math.max(...zote)} and lowest mark : ${Math.min(...zote)}`
  );
  console.log(zote);
}
marksZao();
// question three

function converter() {
  const which = parseInt(
    prompt(`Hi, im your convertor How may i help you?
  1.Convert Kenyan Shillings to Foreign currencies
  2:Convert Foreign currencies to Kenyan Shillings
  `)
  );
  if (which === 1) {
    const amountToConvert = parseInt(
      prompt(`Enter Amount In Kenyan Shillings`)
    );
    // starts here
    if (amountToConvert >= 0 && amountToConvert < 100001) {
      const endelea = parseInt(
        prompt(`what currency do you wish to convert your money to💱?
      1:United Stated Dollar 💵
      2:British Pound Sterling 💷
      3:European Euro 💶
      4:Canadian Dollar 💶
      5:Bahraini Dinar 💷
      6:The UAE dirham 💶
      7.Chinese Yuan 💴
      😄💵💲💸💶💴💷💰
      `)
      );
      if (endelea === 1) {
        convertedAmount = amountToConvert / 118.65;
        alert(
          `You have converted  ${amountToConvert}Ksh to ${convertedAmount} US dollars`
        );
      } //this currency ends here
      else if (endelea === 2) {
        convertedAmount = amountToConvert / 141.68;
        alert(
          `You have converted  ${amountToConvert}Ksh to ${convertedAmount} British Pounds`
        );
      } //this currency ends here
      else if (endelea === 3) {
        convertedAmount = amountToConvert / 120.2;
        alert(
          `You have converted  ${amountToConvert}Ksh to ${convertedAmount} Euros`
        );
      } //this currency ends here
      else if (endelea === 4) {
        convertedAmount = amountToConvert / 94.47;
        alert(
          `You have converted  ${amountToConvert}Ksh to ${convertedAmount} Canadian Dollar`
        );
      } //this currency ends here
      else if (endelea === 5) {
        convertedAmount = amountToConvert / 314.68;
        alert(
          `You have converted  ${amountToConvert}Ksh to ${convertedAmount} Dinars`
        );
      } //this currency ends here
      else if (endelea === 6) {
        convertedAmount = amountToConvert / 32.3;
        alert(
          `You have converted  ${amountToConvert}Ksh to ${convertedAmount} UAE dirham`
        );
      } //this currency ends here
      else if (endelea === 7) {
        convertedAmount = amountToConvert / 17.58;
        alert(
          `You have converted  ${amountToConvert}Ksh to ${convertedAmount} Chinese Yuan`
        );
      }
      // ends here
    }
  }
  // converting to kenyan starts here
  else if (which === 2) {
    const amountToConvert = parseInt(prompt(`How much do you wish to convert`));
    if (amountToConvert >= 0 && amountToConvert <= 100001) {
      const toKenyan = parseInt(
        prompt(`what currency do you wish to convert your money from💱?
      1:United Stated Dollar 💵
      2:British Pound Sterling 💷
      3:European Euro 💶
      4:Canadian Dollar 💶
      5:Bahraini Dinar 💷
      6:The UAE dirham 💶
      7.Chinese Yuan 💴
      Sorry this are the only currencies we support currently
      `)
      );
      if (toKenyan === 1) {
        convertedAmount = amountToConvert * 118.65;
        alert(
          `You have converted  ${amountToConvert} US dollars to ${convertedAmount} Ksh`
        );
      } //this currency ends here
      else if (toKenyan === 2) {
        convertedAmount = amountToConvert * 141.68;
        alert(
          `You have converted  ${amountToConvert}British Pounds to ${convertedAmount} Ksh`
        );
      } //this currency ends here
      else if (toKenyan === 3) {
        convertedAmount = amountToConvert * 120.2;
        alert(
          `You have converted  ${amountToConvert}Euros to ${convertedAmount} Ksh`
        );
      } //this currency ends here
      else if (toKenyan === 4) {
        convertedAmount = amountToConvert * 94.47;
        alert(
          `You have converted  ${amountToConvert}Canadian Dollar to ${convertedAmount} Ksh`
        );
      } //this currency ends here
      else if (toKenyan === 5) {
        convertedAmount = amountToConvert * 314.68;
        alert(
          `You have converted  ${amountToConvert}Dinars to ${convertedAmount} Ksh`
        );
      } //this currency ends here
      else if (toKenyan === 6) {
        convertedAmount = amountToConvert * 32.3;
        alert(
          `You have converted  ${amountToConvert}UAE dirham to ${convertedAmount} Ksh`
        );
      } //this currency ends here
      else if (toKenyan === 7) {
        convertedAmount = amountToConvert * 17.58;
        alert(
          `You have converted  ${amountToConvert}Chinese Yuan to ${convertedAmount} Ksh`
        );
      }
    }
  }
}
converter();
